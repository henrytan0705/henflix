

const searchingReducer = (state = {}, action) => {
    Object.freeze(state);

    switch(action.type) {
        // case AF: 
        //     return {}

        default:
            return state
    }
}

export default searchingReducer;