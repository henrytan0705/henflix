{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/node" },
    { "src": "Gemfile", "use": "@vercel/ruby" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "public/$1" }
  ],
  "buildCommand": "./.vercel_build.sh"
}
